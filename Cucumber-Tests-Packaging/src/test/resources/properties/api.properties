# set base URL of api's where stubs are hosted.
base_url = http://localhost:8181/

# set username for api authentication
user_name = admin

# set password for api authentication
password = admin

# set terminal id for api query parameter.
terminal_id = a1